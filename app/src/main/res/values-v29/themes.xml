<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Burr" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/navigationBar</item>
        <item name="android:colorAccent">@color/teal_200</item>

        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Burr.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Burr.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Burr.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>